name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.11", "3.12"]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e ".[dev]"
          python -m pip install "ruff>=0.11.0"

      - name: Lint with ruff
        run: python -m ruff check src/ scripts/ tests/ --exclude 'scripts/archive/'

      - name: Type check with mypy
        run: |
          python -m mypy src/ --ignore-missing-imports --show-error-codes | tee mypy-output.txt
          ERROR_COUNT=$(grep -c "error:" mypy-output.txt 2>/dev/null || echo 0)
          BASELINE=70
          echo "mypy errors: $ERROR_COUNT (baseline: $BASELINE)"
          if [ "$ERROR_COUNT" -gt "$BASELINE" ]; then
            echo "FAIL: new mypy errors introduced ($ERROR_COUNT > baseline $BASELINE)"
            exit 1
          fi
          echo "mypy baseline check passed (errors <= $BASELINE)"

      - name: Test with pytest
        run: python -m pytest --cov=src --cov-fail-under=62 --cov-report=term-missing

      - name: Check script sizes (>200 lines warning)
        run: |
          echo "Scripts exceeding 200 lines (target: <200):"
          find scripts/ -name "run_*.py" -not -path "scripts/archive/*" -exec wc -l {} + | awk '$1 > 200 {printf "  %s: %d lines\n", $2, $1}' || true
          echo "See docs/plan-05-05 B4 for script slimming plan."

      - name: Smoke test imports
        run: |
          python -c "from src.data_fetcher.db_manager import DuckDBManager"
          python -c "from src.indicators import compute_dktrend, DKTrendParams, TrendMode"
          python -c "from src.signals import Signal"
          python -c "from src.backtest.single_stock import run_single_stock_backtest"
          python -c "from src.notify import WecomWebhookHandler, send_trend_signal"
          python -c "import scripts.run_signal; import scripts.run_backtest_single; import scripts.fetch_stock"
